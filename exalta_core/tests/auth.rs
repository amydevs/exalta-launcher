#[cfg(test)]
mod tests {
    #[tokio::test]
    async fn test_verify() {
        // let client = exalta_core::auth::AuthInfo::default().username_password("", password);
        // client.login("sds", "sdsd").await.unwrap().v;
    }
}
