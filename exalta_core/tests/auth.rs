#[cfg(test)]
mod tests {
    #[tokio::test]
    async fn test_verify() {
        let client = exalta_core::ExaltaClient::new().unwrap();
        // client.login("sds", "sdsd").await.unwrap().v;
    }
}
